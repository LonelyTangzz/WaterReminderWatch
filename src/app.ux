<script>
/**
 * 应用入口 — 生命周期钩子。
 *
 * 提醒由各页面自行管理（前台 setInterval），app 级别仅做最轻量的
 * 生命周期日志。后台保活经实测无效（S4 Vela 平台硬限制），已移除。
 *
 * 平台限制详见 CHANGELOG.md「2026-05-22 — 后台提醒结论」。
 */
export default {
  onCreate() {
    console.log('[app] created')
  },

  onDestroy() {
    console.log('[app] destroyed')
  }
}
</script>
