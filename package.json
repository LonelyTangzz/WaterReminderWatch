{
  "name": "water-reminder-watch",
  "version": "1.0.0",
  "description": "喝水提醒 - 小米 Watch S4（Vela / 快应用）",
  "engines": {
    "node": ">=8.10"
  },
  "scripts": {
    "start": "aiot start --watch",
    "build": "aiot build --enable-jsc",
    "release": "aiot release --enable-jsc --complete-feature",
    "lint": "eslint --format codeframe --fix --ext .ux,.js src/"
  },
  "devDependencies": {
    "@aiot-toolkit/jsc": "^1.0.3",
    "aiot-toolkit": "^2.0.5"
  }
}
